# Apply Progress: premium-coffee-shop — PR 6

## Completed Tasks

- [x] 6.1 OpenGraph metadata — root layout with OG + Twitter + icons, all page metadata verified
- [x] 6.2 JSON-LD LocalBusiness schema — `<script>` in root layout body
- [x] 6.3 sitemap.ts — 4 routes (/, /menu, /faq, /contact)
- [x] 6.4 robots.ts — allow all, disallow /api/, point to sitemap
- [x] 6.5 not-found.tsx — Custom 404 page with warm editorial copy
- [x] 6.6 loading.tsx — Spinner with "Brewing..." text
- [x] 6.7 Favicon — SVG favicon + metadata reference in layout
- [x] 6.8 Accessibility verification — all ARIA already correct, added aria-label to Hero
- [x] 6.9 Reduced motion verification — CSS `@media (prefers-reduced-motion: reduce)` in globals.css

## Files Changed

| File | Action | Description |
|------|--------|-------------|
| `app/layout.tsx` | Modified | Added OG, Twitter, icons metadata; JSON-LD script; metadataBase; changed template separator |
| `app/globals.css` | Modified | Added `@media (prefers-reduced-motion: reduce)` rules |
| `components/sections/hero.tsx` | Modified | Added `aria-label="Hero"` to section |
| `app/sitemap.ts` | Created | Sitemap with 4 static routes |
| `app/robots.ts` | Created | Robots.txt config |
| `app/not-found.tsx` | Created | Custom 404 page with heading, copy, CTA |
| `app/loading.tsx` | Created | Loading spinner with "Brewing..." |
| `public/favicon.svg` | Created | SVG favicon (espresso circle + "C") |

## Line Count

Total changed lines: **128** (budget: <300 ✓)

## Verification

| Check | Result |
|-------|--------|
| `npm run build` | ✅ Zero errors, zero warnings |
| OG tags in `<head>` | ✅ Root layout + all 3 pages (menu, faq, contact) |
| JSON-LD script | ✅ Present in all pages via root layout |
| `/sitemap.xml` | ✅ Generated by Next.js |
| `/robots.txt` | ✅ "User-Agent: * / Allow: / / Disallow: /api/ / Sitemap: https://coffeewerk.com/sitemap.xml" |
| Custom 404 | ✅ `/_not-found` route generated |
| Loading | ✅ `app/loading.tsx` created |
| Favicon | ✅ `public/favicon.svg` + metadata in layout |
| a11y fixes | ✅ Hero `aria-label`, ThemeToggle & hamburger already had aria-labels, FAQ accordion already had full ARIA support |
| Reduced motion | ✅ CSS `@media` rules + Framer Motion `useReducedMotion()` in scroll-reveal & stagger-container |

## Notes

- Pages (menu, faq, contact) already had OG metadata from PR 5 — no changes needed
- Accessibility was already strong: ThemeToggle had `aria-label`, hamburger had `aria-label`, FAQ had `aria-expanded`/`aria-controls`/`role="region"`/keyboard nav, contact form had `<label htmlFor>`, gallery images had `alt` text
- Reduced motion was already handled in `scroll-reveal.tsx` and `stagger-container.tsx` via `useReducedMotion()` — only missing piece was the CSS `.animate-fade-in` and global animation kill-switch
